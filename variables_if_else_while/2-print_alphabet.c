#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)

{
	char *alphabet
	alphabet = "abcdefghijklmnopqrstuvwxyz"
	putchar(alphabet);
	putchar("\n");
	return (0);
}
